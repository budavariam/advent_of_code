import { checkResult } from "../utils/checkResult";
import { solution } from "./solution";

const data = [
    {input: ["ULL", "RRDDD", "LURDL", "UUUUD"], expected: "1985"},
];

checkResult(solution, data);
